# ==========================
# Backend Environment Config
# ==========================

# Port to run Express server (default 5000)
PORT=5000

# Google Cloud Project ID (your Firebase/Cloud project)
FIREBASE_PROJECT_ID=artisan-app-ae358

# Path to Firebase Admin SDK key (local only!)
# In production (Cloud Run), this is not needed because GCP provides credentials automatically.
GOOGLE_APPLICATION_CREDENTIALS=./src/serviceAccountKey.json

# Optional: Debug logging
DEBUG=false
